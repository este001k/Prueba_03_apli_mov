import { VerRegistroPipe } from './ver-registro.pipe';

describe('VerRegistroPipe', () => {
  it('create an instance', () => {
    const pipe = new VerRegistroPipe();
    expect(pipe).toBeTruthy();
  });
});
